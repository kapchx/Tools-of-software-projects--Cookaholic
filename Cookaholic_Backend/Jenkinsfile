pipeline {

    agent any
    stages {

        stage('Checkout Codebase'){
            steps{
                sh 'ls -lha'
                cleanWs()
                
            }
        }

        stage('Build'){
            steps{
                sh 'git checkout -f 32b76401ebc52bfe4d2e72eb38516e6be32c72ad'
                sh 'ls -lha'
            }
        }

        stage('Test'){
            steps{
                sh 'echo testing'
            }
        }

        stage('Deploy'){
            steps{
                sh 'echo testing' 
            }
        }
    }

}
